# Kayle Platform

`/rust` contains Rust code required for the API.

`/rust/api` contains the Rust API for Kayle.

`/platform` contains other stuff necessary for the platform.

`/packages` contains the things the platform depends on.