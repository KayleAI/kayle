import { BlogLayout } from '@/components/BlogLayout'

export const blog = {
  author: 'Arsen Shkrumelyak',
  date: '2024-04-20',
  title: 'Launching Kayle’s Text Moderation Engine (again)',
  description: 'Kayle began last year, but we had to put the project on hold—now we’re back and we’re launching hard!',
}

export const metadata = {
  title: blog.title,
  description: blog.description,
}

export default (props) => <BlogLayout blog={blog} {...props} />

Kayle began last year, but we had to put the project on hold—now we’re back and we’re launching hard!

## Story

Lil’ intro

We began on the 16th of February 2023...

```js
const a = 1
const b = 2
const c = a + b

console.log(c) // 3
```

## New beginnings

We’re back and we’re launching hard!