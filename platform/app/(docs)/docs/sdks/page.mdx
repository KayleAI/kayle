import { Libraries } from '@/components/docs/Libraries'
import { GenerateSEO } from "@/components/docs/GenerateSEO";

export const metadata = GenerateSEO({
	title: "SDKs",
	description:
		"Kayle offers fine-tuned JavaScript, Ruby, PHP, Python, and Go libraries to make your life easier and give you the best experience when consuming the API.",
  url: "https://kayle.ai/docs/sdks",
});

export const sections = [
  { title: 'Official libraries', id: 'official-libraries' },
]

# Kayle SDKs

<Warn>
  The Kayle SDKs are not yet available.
</Warn>

The recommended way to interact with the Kayle API is by using one of our official SDKs. Today, Kayle offers fine-tuned JavaScript, Ruby, PHP, Python, and Go libraries to make your life easier and give you the best experience when consuming the API. {{ className: 'lead' }}

<Libraries />
