import { AuthResetChangePanel } from "@/components/auth/auth-reset-change";

export default async function ForgotPasswordPage() {
	return (
		<div className="min-h-screen flex justify-center items-center">
			<AuthResetChangePanel />
		</div>
	);
}
