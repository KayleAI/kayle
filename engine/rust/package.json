{
	"name": "kayle-rust",
	"version": "0.0.0",
	"private": true,
	"scripts": {
		"deploy": "wrangler deploy",
		"dev": "wrangler dev --remote",
		"dev:rust": "wrangler dev --remote"
	},
	"devDependencies": {
		"wrangler": "3.51.0"
	}
}
