{
	"name": "kayle-rust",
	"version": "0.0.0",
	"private": true,
	"scripts": {
		"deploy": "wrangler deploy",
		"dev:all": "wrangler dev --remote",
		"dev:api": "wrangler dev --remote"
	},
	"devDependencies": {
		"wrangler": "3.51.0"
	}
}
