{
	"name": "engine",
	"version": "0.1.0",
	"scripts": {
		"dev": "bun run build:dev && cargo run",
		"build:dev": "cargo build",
		"build:release": "cargo build --release",
		"start": "bun run build:release && cargo run --release",
		"infisical": "infisical run -- bun run"
	}
}
